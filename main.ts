sprites.onOverlap(SpriteKind.Player, SpriteKind.Player, function on_overlap(sprite: Sprite, otherSprite: Sprite) {
    
})
ControllerButton
animation.runMovementAnimation(null, animation.animationPresets(animation.flyToCenter), 2000, false)
music.ringTone(Note.C)
music.rest(BeatFraction.Half)
effects.blizzard.startScreenEffect()
music.setVolume(20)
info.player1.onScore(100, function on_score() {
    
})
animation.stopAnimation(animation.AnimationTypes.All, null)
if (true) {
    
} else {
    
    while (true) {
        
    }
}

